enum TemperatureUnit { celsius, fahrenheit, kelvin }

enum WindSpeedUnit { kmh, mph, ms, knots }

enum PressureUnit { hpa, inhg, mmhg, mbar }

enum PrecipitationUnit { mm, inches }

enum DistanceUnit { km, miles }
